# PJMTG-automation

会議運営を自動化する Google Apps Script (GAS) プロジェクトです。
**会議の登録・出席者管理・議事録の保存はNotion（既存の議事録DB／出席管理DBを活用）**、
**打刻・議事録投稿・既読/参加率の集計はGoogle側**という役割分担で、以下の一連の流れを自動化します。

1. **会議のスケジュールを決定** — 議事録DBに1ページ登録し、出席管理DBに出席者を紐づけると、Googleカレンダー + Meet会議を自動作成
2. **時間計測ツールで開始打刻** — Meetの実際の参加ログ(入室〜退室時刻)をもとに、**実際に参加した人だけ**を既存の社内活動タイマー(`ActivityTimer.gs`)のメンバーシートに実績時間で自動打刻
3. **議事録機能・録音機能をON** — 会議作成時に Meet の自動録画・自動文字起こし・スマートノート(議事録)をON
4. **会議終了後、議事録と録画を保存・投稿** — Meet APIで生成物が揃い次第、議事録DBのページ(プロパティ+本文)に保存し、Google Chatにも投稿
5. **議事録を既読の人がスタンプを押す** — Google Chatの投稿メッセージへのリアクションを収集し「既読」とみなす
6. **出席/欠席の自動判定 ＋ 参加率・既読率の分析** — Meetの参加ログをもとに出席管理DBの各行へ「出席」「欠席」を自動反映しつつ、招待者・実参加者・既読者を突き合わせて「参加分析」シートに集計

## 前提となるNotionの構成(後輩作成の既存DBをそのまま活用)

既にある2つのデータベースをベースに、プロパティをいくつか追加する形にしています。ゼロから作り直しません。

- **議事録DB**(既存): MTG名 / 日付 / 議事録作成者 / 種別 / ファシリテーター
- **出席管理DB**(既存): MTG(議事録DBへのリレーション) / 名前 / ステータス / 参加チーム
- **メンバーマスタDB**(新規): 氏名 / メールアドレス / 計測ツール氏名 — 社内の人を一度だけ登録しておき、出席管理DBの「メンバー」から都度選ぶだけにする

## なぜこの役割分担なのか

- 議事録の文字起こし・要約は、既に契約している Google Workspace の Meet 機能(自動録画・自動文字起こし・スマートノート)を使う。Notion AIのミーティングノート機能(有料アドオン)は使わない
- 会議の登録・出席者管理・議事録の保存場所はNotion(無料プランのAPI連携で十分。ここに有料のNotion AIは不要)
- 「既読スタンプ」による参加率分析は、Notionのリアクション機能がChatほど強くないため、引き続きGoogle Chatで行う
- 「出席者メール」「打刻対象メンバー」を会議ごとに毎回手入力しなくて済むよう、メンバーマスタDBを介して出席管理DBから選ぶだけにした

## ファイル構成

```
src/
  appsscript.json          マニフェスト(スコープ・APIの設定)
  Utils.gs                 共通ユーティリティ、シート定義
  NotionClient.gs           Notion API連携(議事録DB・出席管理DB・メンバーマスタDBの読み書き)
  00_Setup.gs               初期化(シート作成)
  01_ScheduleMeeting.gs     ① 会議作成(議事録DB+出席管理DB → カレンダー + Meet)
  02_TimeTracking.gs        議事録DB上の会議ステータスの自動遷移(作成済み→開催中→終了)
  03_MeetingArtifacts.gs    ③ Meet API(自動録画/文字起こし/議事録ON、会議記録・参加ログ取得)
  04_PostToChat.gs          ④ 議事録DBへの議事録保存 + Google Chat への投稿
  05_ReactionTracking.gs    ⑤ リアクション(スタンプ)収集
  06_AttendanceAnalysis.gs  ⑥ 出席率/既読率の分析
  07_AttendanceStamping.gs  ② Meet参加ログに基づく実績打刻 + 出席管理DBの出席/欠席自動判定
  ActivityTimer.gs          既存の社内活動打刻タイマー(メンバー/グループ管理、手動修正、サマリー等)
  Main.gs                   メニュー、トリガー一括設定
```

`ActivityTimer.gs` は既存の社内活動タイマーツールをそのまま取り込んだものです。
`onOpen()` はプロジェクト内で1つしか定義できないため、メニュー構築部分だけ
`buildActivityTimerMenu_()` という名前に変更し、`Main.gs` の `onOpen()` から呼び出す形にしています。
それ以外の挙動(グループ管理・手動修正・年度リセット・スマホ打刻など)は変更していません。

## 前提条件

- Google Workspace(有料プラン。自動録画・自動文字起こし・スマートノートが利用できるエディション。録画/文字起こしはBusiness Standard以上、スマートノートはGeminiアドオンが必要な場合があります。管理者が Google 管理コンソールでこれらの機能を有効化している必要があります)
- Notionアカウント(無料プランで可。会議の登録・出席者管理・議事録の保存に使う。Notion AIは不要)
- スクリプトを紐づける Google スプレッドシート — **既存の「活動タイマー」を使っているスプレッドシートと同じもの**に、本プロジェクトのシート・スクリプトを追加する
- 「活動タイマー」側で、打刻対象となる各メンバーの `メンバー_◯◯` シートが作成済みであること(未作成なら「⏱ 活動タイマー」→「⚙ 初期セットアップ」または「➕ メンバーを追加」で作成)
- 管理者操作(メンバー名変更・削除・シート保護・年度リセット)を使う場合は、スクリプトプロパティ `ADMIN_PASSWORD` を設定しておくこと
- スクリプトの実行アカウントが、投稿先の Google Chat スペースのメンバーであること

## セットアップ手順

### 1. Notion側の準備

1. https://www.notion.so/my-integrations で内部インテグレーション(統合)を新規作成し、
   「シークレット」を控える(この文字列がAPIトークンになる)
2. **議事録DB**・**出席管理DB**の両方を、①のインテグレーションと接続(共有)する
3. **議事録DB**の「日付」プロパティで「時刻を含む」「終了日を含む」をONにする
   (このプロパティの開始/終了が、そのままカレンダー予定の開始/終了時刻になる)
4. **議事録DB**に次のプロパティを追加する(既存のプロパティは変更しない)

   | プロパティ名 | 型 |
   |---|---|
   | ステータス | セレクト |
   | 会議ID | テキスト(自動入力) |
   | 会議コード | テキスト(自動入力) |
   | 会議記録ID | テキスト(自動入力) |
   | MeetURL | URL(自動入力) |
   | 実績打刻済み | チェックボックス(自動更新) |
   | 議事録URL | URL(自動入力) |
   | 録画URL | URL(自動入力) |

5. **メンバーマスタDB**を新規作成し、①のインテグレーションと接続する

   | プロパティ名 | 型 |
   |---|---|
   | 氏名 | タイトル(表示用のフルネーム) |
   | メールアドレス | Eメール |
   | 計測ツール氏名 | テキスト(活動タイマー側の「メンバー_◯◯」と完全一致させる) |

   「氏名」と「計測ツール氏名」を分けているのは、活動タイマー側のシート名が
   名字のみの人とフルネームの人が混在しているため。社内の人を一度だけ登録しておく。

6. **出席管理DB**に「メンバー」プロパティ(メンバーマスタDBへのリレーション)を追加する。
   既存の「名前」はそのまま残してよい(ログ表示にのみ使い、活動タイマーとの突き合わせには
   メンバーマスタの「計測ツール氏名」を使う)。
   既存の「ステータス」の選択肢はそのままで構わない(自動化は「出席」「欠席」の2値のみ書き込む)。
7. 各データベースのURル(`https://www.notion.so/xxxxxxxx?v=...` の `xxxxxxxx` 部分)がデータベースID

### 2. Apps Script プロジェクトの作成

1. 対象の Google スプレッドシートを開き、「拡張機能」→「Apps Script」を選択
2. 表示されたプロジェクトに、`src/` 配下の各ファイルの内容をコピーして貼り付ける
   (もしくは [clasp](https://github.com/google/clasp) を使う場合は下記「clasp を使う場合」を参照)
3. `appsscript.json` の内容を反映させる(Apps Scriptエディタの「プロジェクトの設定」→「"appsscript.json" マニフェストファイルをエディタで表示する」を有効化してから編集)
4. 「プロジェクトの設定」→「スクリプトプロパティ」に `NOTION_TOKEN` として、Notion側で控えたシークレットを保存する

### 3. Google Cloud プロジェクトでAPIを有効化

Apps Script に紐づく Google Cloud プロジェクトで、以下のAPIを有効化してください。

- Google Calendar API
- Google Chat API
- Google Meet API (`meet.googleapis.com`)

### 4. Chat スペースIDの確認

投稿先の Google Chat スペースの ID(`spaces/xxxxxxxx` の形式)を控えます。
スペースのURL、または Chat API の `spaces.list` から取得できます。

### 5. 初期化

1. スプレッドシートを再読み込みすると、メニューに「会議自動化」が追加されます
2. 「① 初期化(シート作成)」を実行 → 必要なシートが自動作成されます
3. 「設定」シートの `ChatスペースID` `カレンダーID` `議事録データベースID` `出席管理データベースID` `メンバーマスタデータベースID` を実際の値に書き換える
4. 初回実行時にOAuth権限の承認画面が出るので許可する
5. 「② 定期実行トリガーを設定」を実行 → 各処理が自動実行されるようになります

### 6. 会議の登録

1. **議事録DB**に新規ページを作成し、「MTG名」「日付」(開始〜終了)を入力する
2. **出席管理DB**に、この会議の出席者ぶんだけ行を追加し、「MTG」でこの会議を選び、「メンバー」でメンバーマスタから出席者を選ぶ

あとは自動化が引き継ぎます。「会議ID」「会議コード」「MeetURL」「ステータス」「実績打刻済み」「議事録URL」「録画URL」は
すべてスクリプトが自動で埋めます。

ステータスは `(空)→作成済み→開催中→終了→投稿済み→分析済み` と自動で遷移します
(このステータス自体は時刻だけで進み、打刻とは連動しません)。

## 出席者だけを打刻する仕組み(重要)

打刻は出席管理DBの行(その会議の出席予定者)全員に一律で入るのではなく、**Meetの実際の参加ログ
(入室〜退室時刻)と一致した人だけ**に、実際の参加時間で入ります(`07_AttendanceStamping.gs`)。
同時に、出席管理DBの各行の「ステータス」にも「出席」または「欠席」が自動で書き込まれます。
会議終了後、Meet側の参加記録が確定し次第(10分おきに自動リトライ)まとめて反映される形で、
ライブでの「開始と同時に打刻」ではなく「終了後にまとめて、実際の参加時間で正確に打刻」する方式です。

### なぜ相関表(ユーザーID対応表)が必要か

Meet APIは参加者をメールアドレスや氏名ではなく `users/xxxxxxxx` というGoogleアカウントの
リソースIDでしか返しません(Chat APIのリアクション取得者も同様)。そのため
「ユーザーID対応表」シートで `users/xxxxxxxx → メールアドレス → 氏名(活動タイマー用)` の
対応を管理しています。未知のユーザーIDが検出されると自動で行が追記されるので、
社内の管理者は初回だけ該当欄を埋めれば、以降は自動的に解決されます。

### 制約・注意点

- 電話参加や匿名/ゲスト参加(Googleアカウントを持たない参加者)は自動で紐付けできません。手動修正機能で追記してください。
- 退室→再入室した場合、その間の空白時間は引かれず「最初の入室〜最後の退室」の幅で計算されます。
- 議事録DBページの「実績打刻済み」がONになっている会議は、もう一度は打刻・出席判定されません(二重処理防止)。

## 動作タイミングに関する注意

- 会議終了直後は Meet 側の会議記録・参加ログ・録画/文字起こし/議事録の生成に数分〜十数分かかります。
  `stampActualAttendanceForFinishedMeetings` と `postFinishedMeetingsToChat` はどちらも
  生成が完了するまで自動的に再試行し続けます(10分おき)。
- リアクション集計・参加率分析は1時間おきに実行されるため、即時反映ではありません。
  急ぎ確認したい場合はメニューから手動実行できます。
- Notion APIには利用制限(レートリミット)があります。会議・出席者の件数が極端に多くない
  限り本プロジェクトの呼び出し頻度では問題になりませんが、頻発するようならトリガー間隔を調整してください。

## clasp を使う場合(任意)

```bash
npm install -g @google/clasp
clasp login
cp .clasp.json.example .clasp.json
# .clasp.json の scriptId を実際のスクリプトIDに書き換える
clasp push
```

## 手動実行

スプレッドシートのメニュー「会議自動化」から、各ステップを個別に手動実行することもできます。
「一連の処理を今すぐ全部実行」を選ぶと、会議作成・ステータス更新・実績打刻/出席判定・
Notion保存+Chat投稿・リアクション収集・参加率分析をまとめて1回実行します。
